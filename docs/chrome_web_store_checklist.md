# Chrome Web Store 提出チェックリスト（神シーンメモ）

最終更新: 2026-03-05

## 運用ルール

- このファイルは提出準備の進捗管理に使う。
- 未達は `[ ]`、達成したら必ず `[x]` に変更する。
- 実装変更で状態が変わったら、このファイルも同時更新する。

## 1. パッケージ / Manifest

- [x] `manifest_version` が `3`
- [x] アイコン `16/32/48/128` が定義・存在
- [x] 危険権限（`tabs`, `webRequest`, `scripting` など）未使用（`storage` のみ）
- [x] 本番提出向けに不要な `host_permissions`（`http://localhost/*`, `http://127.0.0.1/*`, `file:///*`）を削除済み

## 2. 実装とポリシー整合

- [x] コメント本文は `chrome.storage.local` 保存
- [x] 外部通信（YouTube oEmbed / ライセンスAPI）の実装を把握済み
- [x] README に外部通信の注記あり
- [x] リモートコード実行（`eval`, `new Function`）未使用
- [ ] 課金APIの実装経路（`verify-device` / `create-checkout-session`）をストア説明文・審査メモで説明済み

## 3. プライバシーポリシー

- [x] ポリシーファイルが存在（`docs/privacy-policy.html`）
- [x] 収集データ、保存場所、第三者提供なしの記述あり
- [x] 問い合わせ先メールアドレスを実値で記載済み
- [x] 公開済みプライバシーポリシーURLを確定し、申請画面へ設定済み

## 4. ストア掲載素材

- [x] スクリーンショット5枚（`1280x800`）を用意
- [ ] プロモ画像（任意）を用意

## 5. CWS コンソール入力（手動）

- [x] ストア説明文（単一目的と主要機能、日本語UIのみ対応）を入力
- [x] ストア説明文に無料/Pro/ベータ特典の条件を入力
- [ ] プライバシー申告（データ収集/利用/送信）を実装に合わせて入力
- [ ] 連絡先メールを入力
- [ ] カテゴリ・言語を設定
- [x] 価格・配布地域・公開範囲を設定
- [ ] 審査メモを入力（必要に応じて権限理由と通信先を補足）

## 6. 提出直前の最終確認

- [ ] ローカルで最新版を読み込み、主要機能（投稿/編集/削除/検索/ジャンプ）を再確認
- [x] 本番提出物にデバッグ用設定（localhost, file URL など）が残っていない
- [ ] 申請情報（説明文・ポリシーURL・画像）が実装と矛盾していない
- [ ] 提出

